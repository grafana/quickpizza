apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-alloy
  labels:
    app.k8s.io/name: grafana-alloy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.k8s.io/name: grafana-alloy
  template:
    metadata:
      name: grafana-alloy
      labels:
        app.k8s.io/name: grafana-alloy
    spec:
      restartPolicy: Always
      serviceAccountName: grafana-alloy
      containers:
        - name: grafana-alloy
          image: grafana/alloy:v1.9.2
          imagePullPolicy: IfNotPresent
          args:
            - run
            - /etc/alloy-config/config.alloy
            - --stability.level=public-preview
          envFrom:
            - secretRef:
                name: grafana-alloy-credentials
          ports:
            - name: grpc
              containerPort: 4317
            - name: http
              containerPort: 4318
          volumeMounts:
            - mountPath: /etc/alloy-config/config.alloy
              name: alloy-config
              subPath: config.alloy
      volumes:
        - name: alloy-config
          configMap:
            name: alloy-config

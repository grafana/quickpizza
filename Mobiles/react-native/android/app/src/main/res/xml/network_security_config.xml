<?xml version="1.0" encoding="utf-8"?>
<!--
  QuickPizza local backend is HTTP (emulator: 10.0.2.2, host: localhost).
  Allow cleartext only for those hosts instead of usesCleartextTraffic="true" globally.
  Custom BASE_URL IPs (e.g. 192.168.x.x) require adding a domain-config entry here.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
